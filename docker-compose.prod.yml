services:
  app:
    read_only: true
    tmpfs:
      - /tmp:size=256M
    healthcheck:
      test: ["CMD", "python", "scripts/container_health.py"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 30s
